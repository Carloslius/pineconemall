server:
  port: 10000

spring:
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://101.35.192.127:3306/pineconemall_pms?useUnicode=true&characterEncoding=UTF-8&serverTimeZone=UTC
    username: root
    password: s300056.

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-value: 1
      logic-not-delete-value: 0
logging:
  level:
    plus.carlosliu.pineconemall: debug